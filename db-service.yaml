apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: nextcloud
  labels:
    app: nextcloud
    component: db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: nextcloud
    component: db
